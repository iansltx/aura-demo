<?php

namespace MyProject\API\Model;

class BaseModel
{
    // Set up logic common to multiple models here, and point dependencies at
    // this class's constructor or setters to avoid having to set them multiple
    // times (once per child model).
}
